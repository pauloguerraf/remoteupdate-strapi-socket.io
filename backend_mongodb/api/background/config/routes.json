{
  "routes": [
    {
      "method": "GET",
      "path": "/background",
      "handler": "background.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/background",
      "handler": "background.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/background",
      "handler": "background.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
